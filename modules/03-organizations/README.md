# 03. Organizations, members & RBAC

## Goal
Build this product capability completely when its turn is reached.

## Dependencies
Previous completed modules only.

## Required work
- confirm product rules and data ownership
- implement/adjust Prisma schema and migration
- implement Express validators, repository, service and routes
- enforce authentication, RBAC and organization scoping
- implement real React/Tailwind states on web
- implement NativeWind mobile surface only if this capability belongs on mobile
- add tests for business rules, permissions, idempotency/failure cases where relevant
- update documentation with actual behavior

## Stop condition
Pass `MODULE-DEFINITION-OF-DONE.md`, then stop and wait for instruction before starting the next module.
